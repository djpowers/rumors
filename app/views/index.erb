<div class="row">
  <div class="col text-center">
    <h1><%= @title %></h1>
  </div>
</div>

<div class="row">
  <div class="col text-center">
    <i class="fa fa-glass"></i>
    <h3 class="tagline">Get all the juice... <br><span>in concentrated form</span></h3>
  </div>
</div>

<div class="row">
  <div class="col text-center">
    <form class="" action="/rumors" method="post" data-parsley-validate>
      <textarea name="rumor[body]" data-parsley-trigger="change" required data-parsley-minlength="10" data-parsley-error-message="Your rumor must be at least 10 characters long, please!" id="rumor_body" cols="35" rows="5" placeholder="Submit a rumor" autofocus="autofocus"></textarea><br />
      <div class="row">
        <div class="col text-center">
          <input type="text" name="rumor[submitter]" id="rumor_submitter" placeholder="Name (optional)"><br />
        </div>
      </div>
      <%= csrf_tag %>
      <div class="row">
        <div class="col text-center">
          <button type="submit" class="btn btn-primary btn-lg">Submit Rumor</button>
        </div>
      </div>
    </form>
  </div>
</div>
<div class="row">
  <div class="col text-center">
    <i class="fa fa-twitter"></i>
    <p class="tweet">Or tweet using <b>#patkellyrumors</b></p>
  </div>
</div>
<hr>

<div class="row">
  <div class="col-md-3 col-sm-5 col-xs-7 col center">
    <% @rumors.each do |rumor| %>
      <div class="rumor">
        "<%= rumor.body %>"<br />
        <% if rumor.tweet_id? %>
          <span class="submitted">Submitted by:</span> <a href="https://twitter.com/<%= rumor.submitter %>/status/<%= rumor.tweet_id %>"><%= rumor.submitter %></a>
        <% elsif rumor.submitter? %>
          <span class="submitted">Submitted by:</span> <%= rumor.submitter %>
        <% else %>
          <span class="submitted">Submitted by:</span><span class="anonymous"> Anonymous</span>
        <% end %> at
        <%= rumor.created_at.in_time_zone('US/Eastern').strftime('%I:%M %p %b %e, %Y') %>
      </div>
      <hr>
    <% end %>
  </div>
</div>

<div class="row">
  <div class="col text-center">
    <%= will_paginate @rumors %>
  </div>
</div>
